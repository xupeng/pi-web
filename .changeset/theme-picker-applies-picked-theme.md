---
"@jmfederico/pi-web": patch
---

Fix the Select Theme picker so picking a theme applies it directly instead of resolving through the light/dark pair, and show a single "current" marker on the applied theme instead of separate selected and active markers.
